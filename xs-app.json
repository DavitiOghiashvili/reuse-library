{
  "welcomeFile": "/index.html",
  "authenticationMethod": "none",
  "routes": [
    {
      "source": "^/resources/(.*)",
      "target": "$1",
      "destination": "ui5-lib",
      "csrfProtection": false
    },
    {
      "source": "^/(.*)",
      "target": "$1",
      "localDir": "reuse-lib/webapp",
      "authenticationType": "none"
    }
  ]
}
