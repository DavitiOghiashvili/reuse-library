specVersion: '3.1'
metadata:
  name: reuse.library
type: application

builder:
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: reuse.library
        additionalFiles:
          - xs-app.json
