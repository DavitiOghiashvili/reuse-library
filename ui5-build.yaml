specVersion: '3.1'
metadata:
  name: reuse.library
type: library

builder:
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateLibraryPreload
      configuration:
        archiveName: reuse.library
        additionalFiles:
          - xs-app.json
