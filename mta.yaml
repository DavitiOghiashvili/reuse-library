_schema-version: "3.1"
ID: reuse.library
version: 0.0.1

modules:
  - name: reuse.library
    type: html5
    path: .
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build
      supported-platforms: []

  - name: reuse.library-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: reuse.library-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - name: reuse.library
          artifacts:
            - reuse.library.zip
          target-path: resources/

  - name: reuse.library-destination-content
    type: com.sap.application.content
    requires:
      - name: reuse.library-destination-service
        parameters:
          content-target: true
      - name: reuse.library-repo-host
        parameters:
          service-key:
            name: reuse.library-repo-host-key
      - name: reuse.library-uaa
        parameters:
          service-key:
            name: reuse.library-uaa-key
    parameters:
      content:
        instance:
          destinations:
            - Name: reuse.library_html_repo_host
              ServiceInstanceName: reuse.library-html5-srv
              ServiceKeyName: reuse.library-repo-host-key
              sap.cloud.service: reuse.library
            - Name: reuse.library-uaa
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: reuse.library-xsuaa-service
              ServiceKeyName: reuse.library-uaa-key
              sap.cloud.service: reuse.library
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true

resources:
  - name: reuse.library-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Name: ui5
                Authentication: NoAuthentication
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
                HTML5.DynamicDestination: true
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: reuse.library-destination-service
      service-plan: lite

  - name: reuse.library-uaa
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: reuse.library-xsuaa-service
      service-plan: application

  - name: reuse.library-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: reuse.library-html5-srv
      service-plan: app-host

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true