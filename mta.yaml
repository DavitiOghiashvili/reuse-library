_schema-version: "3.1"
ID: reuseLibrary
version: 0.0.1

modules:
  - name: reuseLibrary
    type: html5
    path: .
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build
      supported-platforms: []

  - name: reuseLibrary-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: reuseLibrary-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - name: reuseLibrary
          artifacts:
            - reuseLibrary.zip
          target-path: resources/

  - name: reuseLibrary-destination-content
    type: com.sap.application.content
    requires:
      - name: reuseLibrary-destination-service
        parameters:
          content-target: true
      - name: reuseLibrary-repo-host
        parameters:
          service-key:
            name: reuseLibrary-repo-host-key
      - name: reuseLibrary-uaa
        parameters:
          service-key:
            name: reuseLibrary-uaa-key
    parameters:
      content:
        instance:
          destinations:
            - Name: reuseLibrary_html_repo_host
              ServiceInstanceName: reuseLibrary-html5-srv
              ServiceKeyName: reuseLibrary-repo-host-key
              sap.cloud.service: reuseLibrary
            - Name: reuseLibrary-uaa
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: reuseLibrary-xsuaa-service
              ServiceKeyName: reuseLibrary-uaa-key
              sap.cloud.service: reuseLibrary
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true

resources:
  - name: reuseLibrary-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Name: ui5
                Authentication: NoAuthentication
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
                HTML5.DynamicDestination: true
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: reuseLibrary-destination-service
      service-plan: lite

  - name: reuseLibrary-uaa
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: reuseLibrary-xsuaa-service
      service-plan: application

  - name: reuseLibrary-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: reuseLibrary-html5-srv
      service-plan: app-host

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true